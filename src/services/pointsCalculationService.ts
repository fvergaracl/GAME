import {
  defaultPointsTaskCampaign,
  formulaBased,
  formulaBasedMaxBonus,
  defaultPointsTaskCampaignPlusMinorBonus,
} from "../utils/pointsCalculationService";

function calculatePointsForUser(
  user: any,
  game: any,
  taskCompletionTime: number
): number {
  let points = 0;

  return points;
}

export { calculatePointsForUser };
