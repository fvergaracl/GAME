// Shared lifecycle-status constants for custom strategies.
//
// The DRAFT/PUBLISHED/ARCHIVED -> CoreUI badge colour mapping used to be
// copy-kept-in-sync across the editor, library and version-history views;
// it now lives here so the three render the same colours from one source.
export const STATUS_BADGE = {
  DRAFT: 'secondary',
  PUBLISHED: 'success',
  ARCHIVED: 'dark',
}
