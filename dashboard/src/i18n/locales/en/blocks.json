{
  "tooltips": {
    "gd_rule": "A scoring rule: if the condition is met, execute the actions.",
    "gd_compare": "Compares two values with an operator (<, <=, ==, !=, >=, >).",
    "gd_and": "Both conditions must hold for the rule to fire.",
    "gd_or": "At least one of the conditions must hold.",
    "gd_not": "Negates the condition: true becomes false and vice versa.",
    "gd_field": "Read a precomputed analytics field (e.g. measurement count).",
    "gd_field_data": "Read data.<key> from the payload the integration sends.",
    "gd_literal_number": "A fixed number (e.g. 5, 1.5, 0).",
    "gd_literal_text": "A fixed text in quotes.",
    "gd_arith": "Arithmetic operation: +, -, *, /, min or max.",
    "gd_func_call": "Function: int(x) truncates decimals; clamp(value, lo, hi) bounds the value.",
    "gd_assign_points": "Assigns points to the user and ends the rule. The case is the result label.",
    "gd_set_callback_data": "Attaches additional data to the result for the integration to consume.",
    "gd_pre_rule": "PRE rule: runs BEFORE the parent. Can mutate data or veto.",
    "gd_post_rule": "POST rule: runs AFTER the parent. Can read parent.points.",
    "gd_set_data": "Writes data.<key> before calling the parent.",
    "gd_veto": "Aborts the whole assignment. Neither the parent nor the post-rules run.",
    "gd_set_points": "Overrides the points computed by the parent. Does not end the rule.",
    "gd_set_case_name": "Overrides the caseName returned by the parent.",
    "gd_field_parent": "Reads the parent's result. Only valid inside POST rules.",
    "gd_parent_variable_override": "Override a variable_* of the parent before its execution.",
    "gd_rule_elseif": "Add an 'else if' branch: an extra condition checked when the ones above fail.",
    "gd_rule_else": "Add an 'else' branch: runs when no condition above matched."
  },
  "labels": {
    "when": "when",
    "then": "then",
    "elseIf": "else if",
    "else": "else",
    "and": "and",
    "andAlso": "and also",
    "or": "or",
    "orAlso": "or also",
    "not": "not",
    "field": "field",
    "data": "data.",
    "value": "value",
    "min": "min",
    "max": "max",
    "assignPoints": "assign points",
    "case": "case",
    "setCallback": "set data",
    "equals": "=",
    "preWhen": "PRE — when",
    "postWhen": "POST — when",
    "setData": "set data",
    "vetoCase": "VETO — case",
    "setPoints": "set points",
    "setCaseName": "set caseName",
    "parent": "parent.",
    "override": "override"
  }
}
