// Vitest setup: registers jest-dom matchers (toBeInTheDocument, etc.)
// against the Vitest expect global. Imported once per test run via
// vitest.config.mjs setupFiles.
import '@testing-library/jest-dom/vitest'
