# GAME - Domain Model

The domain model and its entity-relationship diagram now live in one place -
the [domain-model](source/domain-model.rst) page of the documentation site,
where the ERD is rendered from Mermaid and lists every table in `app/model/`
(its columns, keys, and relationships). This file is a thin pointer so a second
copy never drifts from the schema.
